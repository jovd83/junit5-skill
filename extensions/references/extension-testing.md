# Extension Testing

1. Add focused tests that prove registration, callback execution, and failure behavior.
2. Test both the intended happy path and at least one misuse or edge case when the extension contract is non-trivial.
3. Keep extension tests small and explicit so lifecycle ordering remains visible.
