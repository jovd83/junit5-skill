# Release Checklist

1. Confirm every `SKILL.md` has valid frontmatter.
2. Confirm every `description` starts with `Use when`.
3. Confirm package links resolve correctly.
4. Confirm helper scripts run in the target shell.
5. Confirm the root skill routes to every published subskill.
6. Confirm the package still reflects JUnit 5 scope and wording.
7. Confirm `agents/openai.yaml` matches the package entrypoint.
